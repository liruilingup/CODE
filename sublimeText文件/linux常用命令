linux常用命令

tail -n 200 filename

scp /// hadoop@140.143.4.113:/home/hadoop

netstat命令：
	用来打印Linux中网络系统的状态信息，
	netstat -au(或者-tunlp | grep ...)
ps命令：
	用于显示当前进程 (process) 的状态，
	ps -ef 或者 -aux

cd命令:
	用于切换当前工作目录至 dirName(目录参数)
	"~" 也表示为 home 目录 的意思，"." 则是表示目前所在的目录，".." 则表示目前目录位置的上一层目录。

ssh协议:
	专为远程登录会话和其他网络服务提供安全性的协议
	ssh root@143.140.4.113
lsblk命令:
	用于列出所有可用块设备的信息，而且还能显示他们之间的依赖关系
	查看的是block device,也就是逻辑磁盘大小。或者使用ls /dev/sd*查看
df命令：
	查看的是file system, 也就是文件系统层的磁盘大小，df -h :查看当前硬盘使用情况。
du命令：
	du命令也是查看使用空间的，但是与df命令不同的是Linux du命令是对文件和目录磁盘使用的空间的查看
fdisk命令：
	是一个创建和维护分区表的程序，它兼容DOS类型的分区表、BSD或者SUN类型的磁盘列表。fdisk -l命令看当前磁盘信息。
rm命令：
	删除一个目录中的一个或多个文件或目录
	-r, 指示rm将参数中列出的全部目录和子目录均递归地删除。
	-f, 强行删除文件。不再提示是否删除

查看用户和组信息：
	cat /etc/group查看所有组信息
	groups 查看当前登录用户的组内成员
	groups test 查看test用户所在的组,以及组内成员
	whoami 查看当前登录用户名

.gz .tar.gz .tar文件的区别


ll -a命令可以查看带.的文件

